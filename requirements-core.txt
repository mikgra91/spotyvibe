# Runtime-only dependencies for macOS / Linux launcher (start.sh)
# For full development/build dependencies (Windows desktop + testing), use requirements.txt
spotipy>=2.23,<3.0
python-dotenv>=1.0,<2.0
flask>=3.0,<4.0
markdown>=3.4,<4.0
keyring>=25.0,<26.0

